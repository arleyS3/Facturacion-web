spring:
  config:
    import:
      - optional:file:.env[.properties]
      - optional:file:./src/backend/api/.env[.properties]
      - optional:file:${user.dir}/src/backend/api/.env[.properties]
  application:
    name: central-fianzas

  # DataSource (PostgreSQL / Supabase)
  datasource:
    url: ${DB_URL}
    driver-class-name: org.postgresql.Driver
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    hikari:
      maximum-pool-size: 2
      minimum-idle: 1
      connection-timeout: 30000
      idle-timeout: 600000
      max-lifetime: 1800000
      initialization-fail-timeout: 10000
      leak-detection-threshold: 60000
      data-source-properties:
        preferQueryMode: simple
        ApplicationName: central-fianzas-api
        prepareThreshold: 0

  # JPA / Hibernate
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
    show-sql: true
    open-in-view: false
    properties:
      hibernate:
        format_sql: true
        connection:
          autocommit: true
springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui

server:
  port: 8080
  forward-headers-strategy: framework

sunat:
  api:
    base-url: ${SUNAT_API_URL}
    token: ${SUNAT_API_TOKEN}

# Encriptación de datos sensibles (contraseñas de certificados)
app:
  encryption:
    key: ${ENCRYPTION_KEY}
  # certificate.default-alias: Eliminado - en prod viene de BD (tabla configuracion_certificado)

jwt:
  secret: ${JWT_SECRET}

# OSE (Operador de Servicios Electrónicos) - SUNAT/OSE SOAP Webservices
ose:
  endpoint:
    url: ${OSE_ENDPOINT_URL:}
  credentials:
    username: ${OSE_USERNAME:}
    password: ${OSE_PASSWORD:}
  soap-action:
    send-bill: ${OSE_SOAP_ACTION_SEND_BILL:sendBill}
    get-status: ${OSE_SOAP_ACTION_GET_STATUS:getStatus}
